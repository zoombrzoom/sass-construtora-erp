rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Qualquer usuário autenticado pode ler e escrever (app controla permissões)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
